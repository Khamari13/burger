<h1>Burgers!</h1>

<h2>Burgers that are not DEVORED!</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  <li>
    {{burger_name}}
    <button type="button" id="chng-Devour" class="btn btn-secondary btn-lg" value={{id}}>
      {{#unless sleep}}Devour IT!{{/unless}}
    </button>
  </li>
  {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that were DEVOURED!</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  <li>
    {{burger_name}}
    <button type="button" id="change-Devour" class="btn btn-secondary btn-lg" value={{id}}>
      {{#unless sleep}}Devour IT!{{/unless}}
    </button>
  </li>
  {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <button type="submit">Add burger</button>
</form>